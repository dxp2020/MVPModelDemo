apply plugin: 'com.android.library'
apply from: "${GRADLES.baseConfig}"


dependencies {
    api fileTree(dir: 'libs', include: ['*.jar'])
    api project(':rx1:mvp_architecture')
    api "com.squareup.okhttp3:logging-interceptor:3.9.1"
}
